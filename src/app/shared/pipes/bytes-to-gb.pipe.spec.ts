import { BytesToGBPipe } from './bytes-to-gb.pipe';

describe('BytesToGBPipe', () => {
  it('create an instance', () => {
    const pipe = new BytesToGBPipe();
    expect(pipe).toBeTruthy();
  });
});
